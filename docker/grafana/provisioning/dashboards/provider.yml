# Grafana 대시보드 자동 프로비저닝
# /var/lib/grafana/dashboards 에 마운트된 JSON을 기동 시 자동 등록한다.
apiVersion: 1

providers:
  - name: ztg
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
