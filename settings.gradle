// Zero Trust Access Gateway — 멀티모듈 루트
// Phase 0: resource-api만 포함. gateway/pdp/pip/common 모듈은 각 Phase에서 추가한다.
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'zero-trust-gateway'

include 'resource-api'
