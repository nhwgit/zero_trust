// Zero Trust Access Gateway — 멀티모듈 루트
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'zero-trust-gateway'

include 'resource-api'
include 'gateway'
include 'common'
include 'pip'
include 'pdp'
